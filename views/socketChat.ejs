<!doctype html>
<html>
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

   <!-- CSS 첨부하는 곳 -->
   <link rel="stylesheet" href="/public/main.css">
 
   <title>Hello, world!</title>
  </head>
  <body>

    <%- include('nav.html') %>
    <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
      
      <h4 class="ml-2 my-3 text-center">채팅하기</h4>
      
      <div class="container input-group mb-2">
        <input class="form-control" id="searchInput">
        <button class="input-group-append btn btn-danger" id="search">검색</button>
      </div>

      <div class="container">
        <ul class="list-group">          
          <li class="list-group-item">
            <h6>
                1. 단체채팅방
                <button class="btn btn-danger" id="groupRoom">입장하기</button>
            </h6>
            <h6>
                2. 전설템 사고팔기
                <button class="btn btn-danger" id="firstRoom">입장하기</button>
            </h6>
            <h6>
                3. 오늘 10시 ~ 12시 쩔해주실분
                <button class="btn btn-danger" id="secondRoom">입장하기</button>
            </h6>
            <h6>
                4. 독산역 스터디모집
                <button class="btn btn-danger" id="thirdRoom">입장하기</button>
            </h6>
        </li>          
        </ul>
      </div>

        <div class="container input-group mb-2 my-3" id="divDisabled">
            <input class="form-control" id="messageText" disabled>
            <button class="input-group-append btn btn-secondary" id="sendMessage" disabled>전송</button>
        </div>

        <div class="container">
            <h6 id="content"></h6>
        </div>

      

      <script>

        var socket = io();

        // 메세지 출력 위치
        socket.on('broadcast', function(data){
            $('#content').append('<div>' + data + '</div>');
        });

        $('#groupRoom').click(function(){
            $('#divDisabled').find('input').prop('disabled', false);
            $('#divDisabled').find('button').prop('disabled', false);
            
            $('#sendMessage').click(function() {
                socket.emit('groupRoom', $('#messageText').val())
            }); 
        });

        $('#firstRoom').click(function(){
            $('#divDisabled').find('input').prop('disabled', false);
            $('#divDisabled').find('button').prop('disabled', false);
                        
            $('#sendMessage').click(function() {
                socket.emit('firstRoom', $('#messageText').val())
            }); 
        });

        $('#secondRoom').click(function(){
            $('#divDisabled').find('input').prop('disabled', false);
            $('#divDisabled').find('button').prop('disabled', false);
                        
            $('#sendMessage').click(function() {
                socket.emit('secondRoom', $('#messageText').val())
            }); 
        });

        // 단체메세지 전송
        // $('#sendMessage').click(function() {
        //     socket.emit('userSend', $('#messageText').val())
        // });        

      </script>

      <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
  </body>
</html>